/* 
Validate the user against database
return values: {
  error
  user DNE (Should indicate that the user needs to make a new account)
  user (User did exist and it will be returned)
}
*/
import react from "react";

export default function useValidateUser() {
  
}